{
  "buildCommand": "echo 'no build: api/mcp/[secret].js is a committed bundle, CI-verified fresh'",
  "outputDirectory": "public",
  "functions": {
    "api/**/*.js": {
      "maxDuration": 30
    }
  }
}
