{
  "name": "@beckwithbarrow/cms-mcp",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Layer B — MCP server exposing the safe-write CMS client as tools. Holds no token logic of its own.",
  "scripts": {
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@beckwithbarrow/cms-client": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.9.3",
    "vitest": "^2.1.9"
  }
}
